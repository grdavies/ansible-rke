# roles/deploy_rke/tasks/config_docker_daemon.yml
---
  - name: /etc/docker/daemon.json
    template:
      src: templates/docker_daemon.json.j2
      dest: /etc/docker/daemon.json
      owner: root
      group: root
      mode: '0644'
    notify: "restart docker"
    become: yes
